version: '3.8'

services:
  fleet-monitor:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HEARTBEAT_TIMEOUT_SECONDS=30.0
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
