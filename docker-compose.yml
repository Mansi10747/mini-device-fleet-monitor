version: '3.8'

services:
  fleet-monitor:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - HEARTBEAT_TIMEOUT_SECONDS=30.0
    restart: unless-stopped
